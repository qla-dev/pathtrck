import { Building2 } from 'lucide-react'; import { api } from '../../services/api'; import { Language } from '../../types'; import { ApiCollectionPage } from './ApiCollectionPage';
export const CompanyWorkspaceView = ({ lang: _lang }: { lang: Language }) => <ApiCollectionPage title="Company Overview" description="Company data, users and fleet loaded from Laravel." empty="No company is connected to this account." icon={Building2} request={api.companies.list} render={(row) => <div className="flex justify-between gap-4"><div><p className="font-bold dark:text-white">{String(row.name || `Company ${row.id}`)}</p><p className="text-sm text-slate-500">{String(row.city || '')} {String(row.country_code || '')}</p></div><p className="text-sm font-bold text-primary">{String(row.status || '—')}</p></div>} />;
