import { Globe2 } from 'lucide-react'; import { api } from '../../services/api'; import { Language } from '../../types'; import { ApiCollectionPage } from './ApiCollectionPage';
export const NetworkView = ({ lang: _lang }: { lang: Language }) => <ApiCollectionPage title="Global Network" description="Registered logistics companies from Laravel." empty="No companies in the network database." icon={Globe2} request={api.companies.list} render={(row) => <div><p className="font-bold dark:text-white">{String(row.name || `Company ${row.id}`)}</p><p className="text-sm text-slate-500">{String(row.country_code || '—')}</p></div>} />;
