import { Users } from 'lucide-react'; import { api } from '../../services/api'; import { Language } from '../../types'; import { ApiCollectionPage } from './ApiCollectionPage';
export const CompanyTeamView = ({ lang: _lang }: { lang: Language }) => <ApiCollectionPage title="Team & Permissions" description="Memberships and roles linked to company users in Laravel." empty="No company team memberships in the database." icon={Users} request={api.companyMemberships.list} render={(row) => <div><p className="font-bold dark:text-white">{String((row.user as { name?: string } | undefined)?.name || `Membership ${row.id}`)}</p><p className="text-sm text-slate-500">{String(row.company_role || '—')} · {String(row.status || '—')}</p></div>} />;
