import { NotebookPen } from 'lucide-react'; import { api } from '../../services/api'; import { Language } from '../../types'; import { ApiCollectionPage } from './ApiCollectionPage';
export const LoadNotesView = ({ lang: _lang }: { lang: Language }) => <ApiCollectionPage title="Load Notes" description="Notes linked to loads and authors through database foreign keys." empty="No load notes in the database." icon={NotebookPen} request={api.notes.list} render={(row) => <div><p className="font-bold dark:text-white">{String(row.body || '')}</p><p className="text-sm text-slate-500">Priority: {String(row.priority || '—')} · {row.is_private ? 'Private' : 'Shared'}</p></div>} />;
