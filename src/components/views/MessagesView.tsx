import { MessagesSquare } from 'lucide-react'; import { api } from '../../services/api'; import { Language } from '../../types'; import { ApiCollectionPage } from './ApiCollectionPage';
export const MessagesView = ({ lang: _lang }: { lang: Language }) => <ApiCollectionPage title="Messages" description="Conversations, participants and messages loaded from Laravel." empty="No conversations in the database." icon={MessagesSquare} request={api.conversations.list} render={(row) => <div><p className="font-bold dark:text-white">{String(row.subject || `Conversation ${row.id}`)}</p><p className="text-sm text-slate-500">{String(row.channel || 'inapp')} · {Array.isArray(row.messages) ? row.messages.length : 0} messages</p></div>} />;
