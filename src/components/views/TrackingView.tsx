import { MapPinned } from 'lucide-react'; import { api } from '../../services/api'; import { Language } from '../../types'; import { ApiCollectionPage } from './ApiCollectionPage';
export const TrackingView = ({ lang: _lang }: { lang: Language }) => <ApiCollectionPage title="Global Tracking" description="Live shipments, locations and tracking events from Laravel." empty="No shipments are being tracked." icon={MapPinned} request={api.shipments.list} render={(row) => <div><p className="font-bold dark:text-white">{String(row.tracking_number || `Shipment ${row.id}`)}</p><p className="text-sm text-slate-500">{String(row.carrier || 'No carrier')} · {String(row.status || '—')}</p></div>} />;
