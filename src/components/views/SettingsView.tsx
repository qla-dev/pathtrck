import { Language, Role } from '../../types'; import { AccountView } from './AccountView'; export const SettingsView = ({ role: _role, lang: _lang, onLogout }: { role: Role; lang: Language; onLogout: () => void }) => <AccountView title="Account Settings" onLogout={onLogout} />;
