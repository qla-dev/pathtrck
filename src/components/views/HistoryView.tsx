import { History } from 'lucide-react'; import { api } from '../../services/api'; import { Language } from '../../types'; import { ApiCollectionPage } from './ApiCollectionPage';
export const HistoryView = ({ lang: _lang }: { lang: Language }) => <ApiCollectionPage title="Route History" description="Completed and historical routes from Laravel." empty="No route history in the database." icon={History} request={api.routes.list} render={(row) => <div><p className="font-bold dark:text-white">{String(row.route_code || `Route ${row.id}`)}</p><p className="text-sm text-slate-500">{String(row.distance_km || 0)} km · {String(row.status || '—')}</p></div>} />;
