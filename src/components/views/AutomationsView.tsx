import { Workflow } from 'lucide-react'; import { Language } from '../../types'; import { Card } from '../ui/Card'; import { InlineDataState } from '../ui/InlineDataState';
export const AutomationsView = ({ lang: _lang }: { lang: Language }) => <Card><div className="mb-4 flex items-center gap-2"><Workflow className="h-5 w-5 text-primary" /><h1 className="text-xl font-black dark:text-white">Automations</h1></div><InlineDataState loading={false} empty="No automations configured." /></Card>;
