import { LayoutDashboard } from 'lucide-react'; import { api } from '../../services/api'; import { Language, Role } from '../../types'; import { ApiCollectionPage } from './ApiCollectionPage';
export const Dashboard = ({ role: _role, lang: _lang }: { role: Role; lang: Language }) => <ApiCollectionPage title="Dashboard" description="Live loads available to your account." empty="No loads in the database." icon={LayoutDashboard} request={api.loads.list} render={(row) => <div className="flex justify-between gap-4"><div><p className="font-bold dark:text-white">{String(row.title || `Load ${row.public_id || row.id}`)}</p><p className="text-sm text-slate-500">{String(row.cargo_type || '—')} · {String(row.transport_type || '—')}</p></div><span className="text-sm font-bold text-primary">{String(row.status || '—')}</span></div>} />;
