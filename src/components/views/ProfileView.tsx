import { Language, Role } from '../../types'; import { AccountView } from './AccountView'; export const ProfileView = ({ role: _role, lang: _lang }: { role: Role; lang: Language }) => <AccountView title="My Profile" />;
