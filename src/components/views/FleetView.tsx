import { Truck } from 'lucide-react'; import { api } from '../../services/api'; import { Language, Role } from '../../types'; import { ApiCollectionPage } from './ApiCollectionPage';
export const FleetView = ({ lang: _lang, role: _role }: { lang: Language; role?: Role }) => <ApiCollectionPage title="Fleet" description="Vehicles, owners, companies and assigned drivers loaded from Laravel." empty="No vehicles in the database." icon={Truck} request={api.vehicles.list} render={(row) => <div className="flex justify-between gap-4"><div><p className="font-bold dark:text-white">{String(row.registration_number || `Vehicle ${row.id}`)}</p><p className="text-sm text-slate-500">{String(row.make || '')} {String(row.model || '')} · {String((row.assigned_driver as { name?: string } | undefined)?.name || 'Unassigned')}</p></div><p className="text-sm font-bold text-primary">{String(row.status || '—')}</p></div>} />;
